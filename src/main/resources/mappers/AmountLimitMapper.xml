<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swpu.uchain.openexperiment.mapper.AmountLimitMapper">

    <insert id="insertOne" useGeneratedKeys="true" keyProperty="id" parameterType="com.swpu.uchain.openexperiment.domain.TimeLimit">
        insert into openexperiment.amount_limit(id, max_amount, min_amount, limit_college, limit_unit)
        values (#{id},#{maxAmount},#{minAmount},#{limitCollege},#{limitUnit})
    </insert>
    <update id="updateTimeLimit">
        update openexperiment.amount_limit set min_amount = #{minAmount},max_amount = #{maxAmount} where id = #{id}
    </update>
    <delete id="deleteLimit">
        delete from openexperiment.amount_limit where id = #{id}
    </delete>
    <select id="getTimeLimitVOByCollegeAndUnit" resultType="com.swpu.uchain.openexperiment.VO.limit.TimeLimitVO">
        select
                max_amount as maxAmount,
                min_amount as minAmount
         from openexperiment.amount_limit
    </select>
</mapper>