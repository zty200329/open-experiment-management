<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.swpu.uchain.openexperiment.mapper.ProjectReviewResultMapper" >
  <resultMap id="BaseResultMap" type="com.swpu.uchain.openexperiment.domain.ProjectReviewResult" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="project_id" property="projectId" jdbcType="BIGINT" />
    <result column="score" property="score" jdbcType="INTEGER" />
    <result column="is_support" property="isSupport" jdbcType="BIT" />
    <result column="reason" property="reason" jdbcType="VARCHAR" />
    <result column="operate_user" property="operateUser" jdbcType="BIGINT" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from project_review
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.swpu.uchain.openexperiment.domain.ProjectReviewResult" >
    insert into project_review ( project_id, score,
      is_support, reason, operate_user)
    values (#{projectId,jdbcType=BIGINT}, #{score,jdbcType=INTEGER},
      #{isSupport,jdbcType=BIT}, #{reason,jdbcType=VARCHAR} ,#{operateUser,jdbcType=BIGINT})
  </insert>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select id, project_id, score, is_support, reason,operate_user
    from project_review
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    select id, project_id, score, is_support,reason, operate_user
    from project_review
  </select>
  <insert id="multiInsert">
    insert into project_review (project_id, score,
    is_support, reason, operate_user)
    value
    <foreach collection="list" item="record" separator=",">
      (
      #{record.projectId,jdbcType=BIGINT},
      #{record.score,jdbcType=INTEGER},
      #{record.isSupport,jdbcType=BIT},
      #{record.reason,jdbcType=VARCHAR},
      #{record.operateUser,jdbcType=BIGINT}
      )
    </foreach>
  </insert>
</mapper>